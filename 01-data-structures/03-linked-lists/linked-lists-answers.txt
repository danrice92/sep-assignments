Spatial locality means that arrays consistently have elements in them stored side by side, which is an advantage of using arrays to iterate through a list of elements.

Linked Lists seem to take a lot longer to add and remove elements but pointing to just one element is faster.